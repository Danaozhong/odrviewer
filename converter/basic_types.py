""""Contains basic types commonly used in the conversion process."""
from enum import Enum


class LaneSide(Enum):
    """An enumeration to differentiate left from right."""

    LEFT = 0
    RIGHT = 1
